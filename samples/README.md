# Sample Assets

This directory holds non-runtime sample files that are useful for manual testing.

- `resumes/`: example resume exports used for upload and interview testing
- `documents/`: other sample documents

Keep new sample PDFs or HTML exports here instead of the repository root.
